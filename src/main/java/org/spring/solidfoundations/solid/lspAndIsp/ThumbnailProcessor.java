package org.spring.solidfoundations.solid.lspAndIsp;

// All these and the rest doesn't mean that we have one interface for every method but related behaviors may be added
public interface ThumbnailProcessor {
    // can place suitable input and output
    void generateThumbnail();
}
